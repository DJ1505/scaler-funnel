# Optional: OpenAI API key for LLM-based resume scoring.
# Without it, the app falls back to rule-based scoring.
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: Port (default 3456)
# PORT=3456
